/*
import {
  ChunkEmbedding,
  LLMChunk,
  LLMClient,
  LLMMessage,
  LLMResponse,
  Tool,
} from '../repository/llm-repository'

class OllamaLLMRepositoryImpl extends LLMClient {
  generateReply(
    model: string,
    messages: LLMMessage[],
    tools?: Tool[],
    options?: Record<string, any>
  ): Promise<LLMResponse> {
    throw new Error('Method not implemented.')
  }
  generateStream(
    model: string,
    messages: LLMMessage[],
    tools?: Tool[],
    options?: Record<string, any>
  ): AsyncGenerator<LLMChunk> {
    throw new Error('Method not implemented.')
  }
  generateEmbedding(
    embeddingModel: string,
    chunks: string[]
  ): Promise<ChunkEmbedding[]> {
    throw new Error('Method not implemented.')
  }
}
*/
