//const auth = require('../../routers/auth.js');

describe('Blank', function () {
  it('test', function () {
    expect('String').toBe('String')
  })
})

/*describe('Get URL origin', function() {
  it('should extract the origin from a given URL', function() {
    expect(auth.getOrigin("https://localhost:8080/test/url")).toBe("localhost");
  });
});*/

/*import file from '../../routers/auth.js';

const getOrigin = file.__get__('getOrigin');
const returnValueOfFoo = getOrigin("https://localhost:8080/test/ing")

describe('getOrigin', function() {
  it('should get origin', function() {
    expect(returnValueOfFoo).toBe("localhost");
  });
});
*/
